# OPERATOR_PANEL

## Current mode
Linked dry-run (target state: evidence-backed limited automation).

## Control links
- Open app-build issues: `https://github.com/<org>/<repo>/issues?q=is%3Aopen+label%3Aapp-build-line`
- In-flight app-build PRs: `https://github.com/<org>/<repo>/pulls?q=is%3Aopen+software-build-control`
- Failing required checks: `https://github.com/<org>/<repo>/pulls?q=is%3Aopen+status%3Afailure`
- Workflow runs: `https://github.com/<org>/<repo>/actions/workflows/app-build-line-smoke.yml`

## Queue and triage
- Confirm issue form completeness.
- Confirm route label is present.
- Confirm `CODEX_TASK_HANDOFF.md` fields are complete.
- Confirm evidence links exist before merge.

## Required attention
- PRs with mixed-scope changed files.
- PRs blocked by stale base or merge conflict.
- PRs missing required evidence links.
- PRs that attempt protected-scope modification.

## Auth and safeguards snapshot
Source of truth: `reports/operator-panel-snapshot.json` generated by `generate_operator_snapshot.py`.

- `OPENAI_API_KEY` configured: read from snapshot file.
- GitHub App installed for automation: manual review required.
- Branch protection active on `main`: manual review required.
- Required checks configured: manual review required.

## Last known good run
Source of truth: `reports/operator-panel-snapshot.json`.

- Replay key: from gate decision timestamp.
- Branch: default branch under review.
- PR: manual link required.
- Evidence artifact: `app-build-line-smoke-evidence`.

## Operator release decision
- Gate result: PASS / HOLD / BLOCKED
- Decision owner:
- Decision timestamp:
- Notes:
