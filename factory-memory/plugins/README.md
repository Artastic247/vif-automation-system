# Plugins

Plugin records capture reusable plugin packs, adapters, packaging notes, and review status.

A plugin is accepted only when its purpose, inputs, outputs, scope, non-goals, validation method, and release state are clear.
